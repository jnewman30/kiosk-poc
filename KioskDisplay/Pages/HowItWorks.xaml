<Page x:Class="KioskDisplay.Pages.HowItWorks"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:vm="clr-namespace:KioskDisplay.ViewModels"
      xmlns:ctl="clr-namespace:KioskDisplay.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" d:DesignHeight="480" d:DesignWidth="640"
      Title="How It Works">
    
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://siteoforigin:,,,/Resources/HowItWorks.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    
    <Page.DataContext>
        <vm:ScrollerViewModel />
    </Page.DataContext>
    
    <DockPanel LastChildFill="True">
        <Border DockPanel.Dock="Top">
            <TextBlock Text="How It Works" Style="{StaticResource PageTitle}" />
        </Border>
        <Border>
            <ContentPresenter Content="{StaticResource ContentImage3}" />
        </Border>
    </DockPanel>
</Page>
